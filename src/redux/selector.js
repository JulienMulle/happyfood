/*import { createSelector } from "@reduxjs/toolkit";

const shopSelector = (state)=> state.shoppinglist;

export const shopTotalSelector = createSelector([shopSelector], (shoppinglist)=>
shoppinglist.reduce((total, current)=> (total += current.quantity), 0)
);*/